apiVersion: v1
kind: Service
metadata:
  name: instagram-api
  namespace: instagram-api
  labels:
    app: instagram-api
spec:
  type: ClusterIP
  selector:
    app: instagram-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
